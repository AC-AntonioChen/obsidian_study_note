## 什么是元数据
元数据是‌**关于代码的数据**‌，例如：
- 类的名称、继承关系、实现的接口
- 方法的名称、参数类型、返回类型、修饰符（`public/private`）
- 字段的类型、注解标记、访问权限
- 注解中定义的键值对（如 `@RequestMapping("/path")`）
## 元数据在哪里
**元数据存在于源码和字节码中**‌：源码中通过语法隐式体现，编译后以结构化形式存储在 `.class` 文件中‌
‌查看元数据需操作字节码‌：反射、javap、ASM 等工具均基于编译后的字节码解析元数据‌