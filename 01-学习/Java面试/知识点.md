1、List 的 contains() 方法中，通过 equals() 方法来判断此对象是否存在于 List 中。
2、枚举类中的对象本质上是一个全局唯一的静态实例，枚举类中的对象无论有多少个引用，都会指向同一个实例。
3、通过重写 equals() 方法可以使两个内存地址不同的对象判断为相等。如果不重写 equals() 方法，类会继承 Object 类的 
4、equals() 方法。在 Object 类中，equals() 直接使用 == 来判断两个对象是否相等。
5、String 类重写了 equals() 方法，比较字符串的值应使用 equals() 方法。
